## Warden: column renamed on `stg_orders.cust_id`

Warden found **8 affected asset(s)**. Lineage coverage is complete for this subgraph, so this list is exhaustive.

### What this change affects

| | Asset | Consequence | Fixed here |
|---|---|---|---|
| 🔴 | `dbt:dim_customers` | breaks | yes |
| 🔴 | `dbt:fct_orders` | breaks | yes |
| 🔴 | `dbt:fct_revenue` | breaks | yes |
| 🔴 | `tableau:customer_health` | breaks | no — different system |
| 🔴 | `tableau:exec_revenue_overview` | breaks | no — different system |
| 🔴 | `tableau:orders_operational` | breaks | no — different system |
| 🔴 | `dbt:customer_ltv` | breaks | yes |
| 🔴 | `python:customer_segments` | breaks | no — different system |

### Why this fix

4 of 8 broken references are dbt models in this repository, so updating them directly is a single atomic change with no transitional state. The remaining 4 live in other systems and need separate coordination.

Also valid: `compatibility_alias`, `two_phase_deprecate`. These differ in their trade-offs, not in correctness — the recommendation above assumes an atomic change is acceptable.

### Needs a human decision

Impacted assets span ['dbt', 'python', 'tableau']. Whether a coordinated breaking change is acceptable this cycle is not recorded in the catalog — confirm with the owning teams.

### Verification

_No code was generated, so nothing was executed._

### What Warden could see

- Reached **9** entities out of **27** declared in the estate
- **100%** of lineage edges were parsed by a connector rather than inferred
- No blind spots detected in this subgraph

---
Generated by [Warden](https://github.com/sjr27-maker/Warden_datahub). Coverage 1.0 against a threshold of 0.6.