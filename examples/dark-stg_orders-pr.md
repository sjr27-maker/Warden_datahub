## Warden: column renamed on `stg_orders.cust_id`

Warden found **at least 4 affected asset(s)**. This is a floor, not a census: `python`, `tableau` have no lineage connector, so consumers there cannot be detected.

### What this change affects

| | Asset | Consequence | Fixed here |
|---|---|---|---|
| 🔴 | `dbt:dim_customers` | breaks | yes |
| 🔴 | `dbt:fct_orders` | breaks | yes |
| 🔴 | `dbt:fct_revenue` | breaks | yes |
| 🔴 | `dbt:customer_ltv` | breaks | yes |

### Why this fix

All 4 broken references are inside this repository, so updating them directly is a single atomic change with no transitional state.

Also valid: `compatibility_alias`, `two_phase_deprecate`. These differ in their trade-offs, not in correctness — the recommendation above assumes an atomic change is acceptable.

### Verification

_No code was generated, so nothing was executed._

### What Warden could see

- Reached **5** entities out of **27** declared in the estate
- **100%** of lineage edges were parsed by a connector rather than inferred

**Blind spots:**
- tableau has no lineage connector configured; 4 entities are invisible, and any consumers among them cannot be detected
- python has no lineage connector configured; 1 entities are invisible, and any consumers among them cannot be detected

---
Generated by [Warden](https://github.com/sjr27-maker/Warden_datahub). Coverage 0.679 against a threshold of 0.6.