name: warden_world
version: "1.0.0"
config-version: 2
profile: warden_world

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  warden_world:
    staging:
      +materialized: view
    marts:
      +materialized: table